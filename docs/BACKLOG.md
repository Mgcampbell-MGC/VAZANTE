# BACKLOG — engineering, in order

Nothing analytical runs before #1. Each item says what "done" means. Dates are the business case's (Part 9.3); owner is the developer unless stated.

1. **Freeze the informe table mapping.** Diff `docs/reference/informe_mensal_202608_headers.txt` and the CVM dictionary against Res. CVM 175 Suplemento G; write `config/informe_mapping.yaml` with a `concepts:` block (carteira, PDD, a-vencer, inadimplentes por faixa, pagos antecipadamente, aquisições, alienações, substituições, recompras, cedentes, PL, cotas/subclasses, rentabilidade, resgates solicitados, liquidez) → `{tab, column}`. Done when `vazante.cvm.schema.column()` resolves every concept the break-flag and the screen need, and the test in `tests/test_cvm_schema.py` asserts the concept list. The doc's estimate: twenty minutes; expect an afternoon.
2. **Settle the Tabela VIII identity question** (`docs/OPEN_NOTES.md` A). Pull one fund's informe as filed on Fundos.NET and check whether the 25 maiores sacados carry CPF/CNPJ there. Done when OPEN_NOTES has the answer with the document pulled, and the D-dimension design is confirmed or redesigned.
3. **Bulk layer as a resident job.** `vazante cvm fetch --months 24` + `cad-fi` daily, into `data/raw`; a duckdb/parquet panel in `data/derived` keyed on CNPJ_FUNDO_CLASSE × DT_COMPTC. Run the fund-versus-class filing test (Tabela VIII Σ Valor / PL vs Σ %PL on 200 funds) and measure how often cedentes are named in Tabela I. Done when the 24-month panel builds from scratch in one command.
4. **The labelled break-date set and BREAK-FLAG.** Master (53 funds), Reag (110), Trustee/Banvox (63) lineage funds with break dates from the BCB decrees; the indicator panel (A–D primaries, secondaries); fit the thresholds in `config/thresholds.yaml`; t_peak / t_stop; V1/V2/V3 partition. Done when the rule is scored on the labelled set and the thresholds carry a date and a hit rate instead of ASSUMPTION.
5. **The Part 3.6 screen** and the supply count query (Part 3.4). Needs the FNET client (#6) for filter (i). Done when it emits the weekly top-20 calling list.
6. **Fundos.NET client** through `pull()`: regulamento, DFs + pareceres, atas, convocações, fatos relevantes, comunicados, lâmina, informe as filed. Done when one fund's twelve months of documents land in `data/raw/fnet/<cnpj>/` with pull records.
7. **Counterparty run** (Receita via Minha Receita, CENPROT, DJEN or a court aggregator, relatedness graph) for the 25 named sacados + named cedentes — ~90 queries, ~40 minutes, ~R$100 per pack. Blocked on #2 for the sacado names and on DJEN access from a Brazilian desk.
8. **Rebuild the economics model** (P5) against the Part 7 tables: funnel, hurdle ladder, capital at risk, 48-month campaign, 2,500 paths per scenario, the three tax characterisations. Regression anchor: `tests/test_economics.py`. Done when the base case reproduces 6.2 closes / R$18.4m pre-tax / R$10.9m after tax within Monte-Carlo noise.
9. **The Day-1 sheet builder** (Appendix B, eighteen fields) from #3–#7. Done when a flagged fund produces the two pages in under an hour of machine time and the human fields are clearly marked.
10. **Oracle on a tape**: tape schema, Tier 0 structural sweep, fingerprint database, cash reconstruction from CNAB, entity graph, recourse extraction, the lift list, buyer routing, max-bid. Starts on Deal 0's first real tape (Prisma's anonymised tape under NDA, or Fictor from public filings). The vocabulary, `pull()`, the bridge and the validators exist.
11. **Buyer Compiler**: calibration answers → YAML box (V0), shadow-portfolio corrections (V1), bids → observed box (V2). The schema and bid log exist; the first real V0 comes from GC's first session.
12. **Thin Streamlit UI for the partners** (UPLOAD → RUN → SEE WHAT'S REAL → SEE WHO BUYS → SEE MAX BID). Last, after #10.

Not in the backlog by design (business case 4.5): sampling engine, case management, collection floor, client-facing product, administration or custody technology, anything resembling a report a seller could pay for.
