"""Economics: the one-trade arithmetic (reference_book) and, to be rebuilt, the 48-month campaign model.

The original P5_model.py (2,500 Monte-Carlo paths per scenario) is lost — it is in neither Drive nor this repo.
Only its outputs survive, in docs/01 Part 7. The rebuilt model must reproduce them; reference_book.py is the first
regression anchor.
"""
