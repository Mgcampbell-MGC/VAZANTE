"""Detection: the bulk layer over CVM open data, the break-flag rule, and the screen that finds Deal 1.

Order of construction (docs/BACKLOG.md): download → freeze the table mapping → indicator panel → BREAK-FLAG →
the Part 3.6 screen → the pre-exclusivity pack. Only `download` is functional today.
"""
